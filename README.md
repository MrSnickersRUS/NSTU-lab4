# 🧵 Лабораторная работа №4: Многопоточное программирование на C++

Изучение механизмов многопоточности, примитивов синхронизации и решения классических проблем параллелизма в современном C++.

---

## 📋 Содержание

- [Описание](#-описание)
- [Задачи](#-задачи)
- [Структура проекта](#-структура-проекта)
- [Технологии](#-технологии)
- [Сборка и запуск](#-сборка-и-запуск)
- [Результаты](#-результаты)
- [Выводы](#-выводы)

---

## 📖 Описание

Проект демонстрирует работу с многопоточностью в C++20, включая:
- Анализ примитивов синхронизации (Mutex, Semaphore, Barrier, SpinLock и др.)
- Параллельную обработку данных с измерением ускорения
- Решение классической задачи "Обедающие философы" без deadlock

---

## 🎯 Задачи

### **Задача 1: Race Condition и примитивы синхронизации**

Реализация параллельного запуска потоков с демонстрацией состояния гонки и её решения через различные примитивы:

| Примитив | Описание | Особенности |
|----------|----------|-------------|
| **No Sync** | Без синхронизации | ⚠️ Race condition - потеря данных |
| **Mutex** | Взаимное исключение | ✅ Блокировка через ОС |
| **Semaphore** | Счётный семафор (C++20) | ✅ Ограничение доступа N потоков |
| **Barrier** | Барьер синхронизации (C++20) | ✅ Групповая точка встречи |
| **SpinLock** | Активное ожидание | ⚡ Быстро для коротких секций |
| **SpinWait** | Очередь по номерам | 🔄 Turn-based синхронизация |
| **Monitor** | Mutex + Condition Variable | 🎛️ Сложная координация |

**Измерения:** Производительность каждого примитива с помощью класса `StopWatch`.

---

### **Задача 2: Многопоточная валидация дат**

```cpp
struct Date {
    int day, month, year;
};
```

**Цель:** Проверить корректность дат в массиве (например, 32 декабря - некорректная дата).

**Реализация:**
- 🔹 Однопоточная обработка
- 🔸 Многопоточная обработка с разделением массива
- 📊 Сравнение времени выполнения и расчёт **Speedup**

**Результат:**
```
Массив: 100,000 дат
Потоков: 4

Однопоточный:    125 мс | Найдено некорректных: 5,847
Многопоточный:    35 мс | Найдено некорректных: 5,847
Ускорение:      3.57x   ✅
```

---

### **Задача 3: Обедающие философы 🍝**

Классическая проблема синхронизации:
- 5 философов за круглым столом
- Между ними 5 вилок
- Философ может есть только с двумя вилками

**Проблема:** Deadlock - каждый берёт левую вилку и ждёт правую → взаимная блокировка.

**Решение:** **Resource Ordering** 
```cpp
// Всегда берём вилки в порядке возрастания номеров
int first_fork = std::min(left_fork, right_fork);
int second_fork = std::max(left_fork, right_fork);

forks[first_fork]->lock();
forks[second_fork]->lock();
// ... едим ...
forks[second_fork]->unlock();
forks[first_fork]->unlock();
```

**Результат:** ✅ Deadlock не происходит, все философы регулярно едят.

---

## 📁 Структура проекта

```
lab4/
├── Makefile                    # Сборка проекта
├── README.md                   # Этот файл
└── src/
    ├── main.cpp                # Точка входа, интерактивное меню
    ├── benchmark.hpp           # Класс StopWatch для замеров времени
    ├── sync_primitives.hpp     # Кастомные примитивы (SpinLock, Monitor)
    ├── task1_race.hpp          # Задача 1: Race Condition
    ├── task2_dates.hpp         # Задача 2: Многопоточная валидация
    └── task3_philosophers.hpp  # Задача 3: Обедающие философы
```

---

## 🛠️ Технологии

- **Язык:** C++20
- **Компилятор:** GCC 13.3+ / Clang 16+
- **Стандартная библиотека:**
  - `<thread>` - управление потоками
  - `<mutex>` - взаимное исключение
  - `<semaphore>` - семафоры (C++20)
  - `<barrier>` - барьеры (C++20)
  - `<atomic>` - атомарные операции
  - `<condition_variable>` - условные переменные
  - `<chrono>` - работа со временем

---

## 🚀 Сборка и запуск

### Требования
- GCC 13+ или Clang 16+ с поддержкой C++20
- Linux / macOS / WSL

### Компиляция
```bash
make
```

### Запуск
```bash
./lab4
```

### Меню программы
```
      Лаб. 4: Многопоточность в C++     

1. Задача 1: Анализ состояния гонки
2. Задача 2: Многопоточная проверка дат
3. Задача 3: Задача обедающих философов
4. Запустить все задачи
0. Выход

Выберите опцию: _
```

### Очистка
```bash
make clean
```

---

## 📊 Результаты

### Производительность примитивов синхронизации

| Примитив | Время (мс) | Корректность | CPU usage |
|----------|------------|--------------|-----------|
| No Sync | 12 | ❌ Race | Low |
| Mutex | 245 | ✅ | Low |
| Semaphore | 248 | ✅ | Low |
| Barrier | 312 | ✅ | Low |
| SpinLock | 187 | ✅ | **High** |
| SpinWait | 892 | ✅ | **High** |
| Monitor | 251 | ✅ | Low |

**Вывод:** SpinLock быстрее для коротких критических секций, но "сжигает" CPU.

---

### Многопоточная обработка данных (Закон Амдала)

```
Speedup = T_sequential / T_parallel

Теоретический максимум: N (количество ядер)
Практический результат: ~3.5x на 4 ядрах (87% эффективность)
```

**Ограничения:**
- Последовательные участки (создание потоков, join)
- Overhead синхронизации
- Переключение контекста

---

### Обедающие философы

```
Количество философов: 5
Длительность симуляции: 10 секунд

Статистика приёмов пищи:
  Философ 0: 23 приёмов пищи
  Философ 1: 21 приёмов пищи
  Философ 2: 24 приёмов пищи
  Философ 3: 22 приёмов пищи
  Философ 4: 20 приёмов пищи
Всего приёмов пищи: 110
Среднее на философа: 22

Дедлока не произошло! ✓
```

---

## 💡 Выводы

### Ключевые результаты

1. **Race Condition - критическая проблема**
   - Без синхронизации многопоточный код даёт некорректные результаты
   - Критические секции обязательны при доступе к общим данным

2. **Выбор примитива зависит от задачи**
   - Mutex - универсальное решение
   - SpinLock - для очень коротких операций
   - Semaphore - для ограничения числа потоков
   - Barrier - для групповой синхронизации

3. **Deadlock решается алгоритмически**
   - Resource Ordering нарушает циклическое ожидание
   - Правильный порядок захвата ресурсов гарантирует прогресс

4. **Параллелизм имеет ограничения**
   - Закон Амдала: ускорение ограничено последовательной частью
   - Overhead создания потоков и синхронизации
   - Оптимально: число потоков ≈ число ядер

5. **C++20 упростил многопоточность**
   - Стандартные `semaphore` и `barrier`
   - RAII гарантирует освобождение ресурсов
   - `std::atomic` для lock-free операций

---

## 📚 Дополнительные материалы

- [C++ Threading Documentation](https://en.cppreference.com/w/cpp/thread)
- [Закон Амдала](https://ru.wikipedia.org/wiki/Закон_Амдала)
- [Dining Philosophers Problem](https://en.wikipedia.org/wiki/Dining_philosophers_problem)
- [C++20 Features](https://en.cppreference.com/w/cpp/20)

⭐ **Если проект был полезен, поставьте звезду!**
